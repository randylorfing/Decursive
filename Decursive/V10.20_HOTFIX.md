# v10.20 Managed Color Hotfix

- Fixed a stale call to the removed `updateManagedPriorityCurves()` helper.
- The live resolver now refreshes `updateManagedAnyPriorityCurve()` correctly.
- Fixed an invalid Blizzard texture sublevel of `8`; the maximum supported sublevel is `7`.
- No visual-design changes were made from v10.19.
- Keeps the two-affliction model: primary aura uses the inner square; a second simultaneous aura uses one border.
